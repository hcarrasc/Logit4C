#include <stdio.h>
#import "logit.h"

int main(){
    
    char* data;
     
    data = "Hello World :)";
    logit(data, _F_, _L_); 
    
    data = "Is an amazing day to coding :D";
    logit(data, _F_, _L_); 
    
    data = "And add a few new features :D";
    logit(data, _F_, _L_); 
        
}
